export default class Menus {
  constructor(menuId, name, price, quantity, type) {
    this.menuId = menuId;
    this.name = name;
    this.price = price;
    this.quantity = quantity;
    this.type = type;
  }
}
